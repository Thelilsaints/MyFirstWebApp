@model IEnumerable<MyFirstWebApp.Models.Product>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-warm">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category.CategoryId)
            </td>
            <td>
                <div class="action-buttons">
                    <a asp-action="Edit" asp-route-id="@item.ProductId" class="btn btn-warm btn-sm">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.ProductId" class="btn btn-warm btn-sm">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.ProductId" class="btn btn-warm-danger btn-sm">Delete</a>
                </div>
            </td>
        </tr>
}
    </tbody>
</table>
